{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    // NOTE: This connection string uses default credentials for development purposes.
    // In production, use environment variables or user secrets to store database credentials.
    "DefaultConnection": "Host=localhost;Port=5432;Database=ttshang;Username=postgres;Password=postgres"
  }
}
